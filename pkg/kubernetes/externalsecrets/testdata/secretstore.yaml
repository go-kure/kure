apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: aws-store
  namespace: default
spec:
  controller: my-controller
  provider:
    aws:
      auth: {}
      region: us-east-1
      service: SecretsManager
