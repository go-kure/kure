apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: web
  namespace: default
spec:
  maxReplicas: 0
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: web
